import React from 'react';

const ImageSlider: React.FC = () => {
  return (
    <div
      style={{
        width: '100%',
        height: '300px',
        backgroundColor: '#f8f9fa',
        marginBottom: '20px',
      }}
    >
      {/* Swiper 컴포넌트는 나중에 구현 */}
      이미지 슬라이더 영역
    </div>
  );
};

export default ImageSlider;
